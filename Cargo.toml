[package]
name = "bevy-ps2"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", default-features = false }
# panic-halt = "1.0.0"
# serde = { version = "1.0", features = ["derive"] }
# crossbeam-channel = "0.5.16"
# serde_json = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
